{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:push": "bunx --bun drizzle-kit push"
  },
  "dependencies": {
    "@libsql/client": "^0.15.8",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.1"
  }
}
